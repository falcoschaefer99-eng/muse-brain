# MUSE Brain

Public release workspace for MUSE Brain.

- `muse-brain/` — Cloudflare Worker memory engine (MCP server)
- `runner/` — autonomous runner/orchestrator layer

Start with `muse-brain/README.md` for product docs and setup.
