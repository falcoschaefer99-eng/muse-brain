# Secrets
.credentials
.env
.env.*
*.key
*.pem

# Node
node_modules/
dist/
.wrangler/

# OS
.DS_Store
# Runner local state/config
runner/state/
runner/config/tenants.json

# Runner session telemetry (leaks cost/model/tenant — never commit)
audit.jsonl
*.jsonl
